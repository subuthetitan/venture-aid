import Placeholder from "../components/Placeholder";
export default function Ledger() {
  return <Placeholder title="Transparency Ledger" owner="C" notes={[
    "District choropleth on the same map component as the Locator",
    "'Prototype data — synthetic' printed on the chart, not in a footnote",
    "k-anonymity: districts under 5 applications suppressed",
    "Point: no Indian government credit scheme publishes this at all",
  ]} />;
}
