import Placeholder from "../components/Placeholder";
export default function SanctionReady() {
  return <Placeholder title="Sanction-Ready" owner="B" notes={[
    "Voice capture — and a 'type it instead' path visible at all times",
    "Bhashini primary, Sarvam fallback, provider shown in the UI",
    "Editable cost sheet from the 8-10 activity templates",
    "WeasyPrint PDF. Test Devanagari conjuncts on day one",
  ]} />;
}
