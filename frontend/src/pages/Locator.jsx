import Placeholder from "../components/Placeholder";
export default function Locator() {
  return <Placeholder title="Partner Locator & Router" owner="C" notes={[
    "MapLibre + static DataMeet district GeoJSON — renders offline",
    "SCA offices and empanelled branches as points",
    "Mappls Directions for routing (the one live external call)",
    "Reachability layer: Telangana and Ladakh dark. That is the argument",
  ]} />;
}
