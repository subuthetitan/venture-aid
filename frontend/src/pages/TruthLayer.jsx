import Placeholder from "../components/Placeholder";
export default function TruthLayer() {
  return <Placeholder title="Scheme Truth Layer" owner="A" notes={[
    "Live contradictions table from the live_contradiction view",
    "Both values, both source URLs, both fetch dates. Never pick one",
    "Registry browser: every rule with its source and observation date",
    "Label the seed honestly: rules entered by hand for the prototype",
  ]} />;
}
