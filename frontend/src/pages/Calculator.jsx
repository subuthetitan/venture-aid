import Placeholder from "../components/Placeholder";
export default function Calculator() {
  return <Placeholder title="Financial Calculator" owner="B" notes={[
    "Fully real from hour 0 — no external dependency",
    "Scheme, project cost, own contribution in; EMI and schedule out",
    "Moratorium handled: interest accrues, principal deferred",
    "Subsidy-delay toggle. This is the differentiator inside a boring feature",
  ]} />;
}
